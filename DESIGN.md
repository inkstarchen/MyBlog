# 认知空间设计原则

## 它为什么存在

普通笔记系统倾向于把搜索结果、最近编辑、标签和文件列表同时推入视野。认知空间解决的不是“怎样更快找到全部内容”，而是“怎样从一个主题出发，逐步恢复与它有关的理解”。

它是一套用于重新进入内容的索引和披露体系。每次只呈现当前层级，让注意力先建立方向，再决定是否继续深入。

## 内容边界

适合进入空间的内容通常同时满足：

- 已经经过一段时间沉淀，不依赖频繁更新；
- 围绕一个相对独立、长期存在的主题；
- 值得在未来重新阅读，而不只是作为事实备查；
- 能够改变、恢复或加深自己对某件事的理解。

以下内容留在普通笔记系统中：

- 技术笔记、操作步骤和故障记录；
- 实验日志、会议记录和过程材料；
- 高频生长、持续修改的草稿；
- 主要依赖分类、筛选或全文搜索才能使用的资料。

## 内容流转

```text
随手捕捉 → 工作笔记 → 反复整理 → 形成稳定主题 → 进入认知空间
```

认知空间不是捕捉入口，而是沉淀之后的目的地。一个主题只有在结构相对稳定时才进入；之后可以修订，但不以持续更新为主要使用方式。

## 信息结构

- 起点只展示 3–7 个长期主题。
- 每个主题只披露它的直接下一级，不提前展示全部后代。
- 中间节点解释“这一层讨论什么”，外围文字提供继续深入的方向。
- 叶子节点承载完整内容，点击中心进入安静的阅读状态。
- 返回上层意味着恢复更宽的观察范围，而不是回到文章列表。

层级应表达理解关系，而不是文件类型。例如使用“自我 → 感知”，而不是“文章 → 心理类文章”。

## 交互原则

1. 渐进披露：每次只让一个层级进入感知。
2. 位置记忆：同一主题的位置保持稳定，帮助形成空间记忆。
3. 克制运动：漂浮只用于表达内容仍有生命，不抢夺阅读注意力。
4. 无全局噪声：默认不出现搜索框、标签云、时间线和最近更新。
5. 随时回退：任何深度都能明确返回，避免陷入没有方向的浏览。

## 视觉方向

整体使用冷白、雾灰和低饱和灰绿。中心像一张悬浮的薄纸，外围细线像水面波纹。留白承担主要结构，颜色只用于表达层次和焦点，不承担装饰任务。

动态应该缓慢、幅度小，并尊重系统的“减少动态效果”设置。正文展开后，空间退到背景，让文字成为唯一重点。

## 在内容中排除一个分支

需要保留在仓库、但不进入认知空间的目录，可以在该目录的 `index.md` 中设置：

```yaml
space: false
```

整个目录及其后代都不会出现在生成结果中，原始 Markdown 文件仍会保留。
